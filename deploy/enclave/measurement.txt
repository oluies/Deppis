# Reproducible enclave measurement — recorded reference values (T057).
#
# Produced by `deploy/enclave/Dockerfile` (static-musl obsd → Gramine SGX-sim sign), verified
# reproducible by the build itself (obsd built twice byte-identical; MRENCLAVE signed twice identical).
# Reproduce / re-verify with `deploy/enclave/reproduce.sh`. These are PUBLIC values (hashes), not secrets.
#
# `attestation.ReproducibleMeasurementSpec` pins MRENCLAVE below and drives it through the transparency
# ReferenceLog publish → inclusion-proof → pinned-root trust chain.
#
# DEV (Constitution IV): signed with a throwaway dev key (so MRSIGNER is a dev value), not published to
# the public transparency log. Not an entitlement to claim metadata privacy.

toolchain      = rust:1.88.0-bookworm  (target x86_64-unknown-linux-musl, static PIE)
gramine        = gramineproject/gramine:stable-jammy  (1.9)
source_epoch   = 1700000000

obsd_sha256    = cb78f33e91fe1b993e4d2e8c7099034ca4c16e5dade66054b12ebf5e353693c0
MRENCLAVE      = 45296a439f2db45b48eec1f4611699f740be707ef5b979f52be7b1a177c6e64f
MRSIGNER       = (dev key, random per build; production pins the PCK-endorsed signer)
