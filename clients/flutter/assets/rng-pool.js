// Secure-RNG pool for the native (flutter_js) engine. Installed BEFORE the engine bundle so that
// `new ProtocolEngine()` (which mints a per-session cover key) finds `crypto.getRandomValues`, which
// JavaScriptCore otherwise lacks. The pool bytes are OS CSPRNG, generated by Dart `Random.secure()`
// and passed in as base64; this file ONLY decodes and serves them — there is no PRNG here (every byte
// is OS entropy; Constitution I/II). The pool is finite: exhausting it throws a DISTINCT, recognizable
// error rather than returning low-quality bytes.
//
// Single source of truth: `engine_factory_io.dart` loads this exact file at runtime, and
// `protocol-core-js/e2e/rng-pool.cjs` loads it in CI-style Node to test the decoder + serve/exhaust
// behaviour against known pool sizes — so the size math can't silently drift.
globalThis.__installSecureRandomPool = function (b64) {
  var T = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/';
  var lut = {};
  for (var i = 0; i < T.length; i++) lut[T.charAt(i)] = i;
  var clean = b64.replace(/=+$/, ''); // tolerate optional padding (atob is absent in JSC/QuickJS)
  var pool = new Uint8Array((clean.length * 6) >> 3);
  var acc = 0, bits = 0, o = 0;
  for (var j = 0; j < clean.length; j++) {
    acc = (acc << 6) | lut[clean.charAt(j)];
    bits += 6;
    if (bits >= 8) { bits -= 8; pool[o++] = (acc >> bits) & 0xff; }
  }
  var off = 0;
  globalThis.crypto = {
    getRandomValues: function (arr) {
      var n = arr.length;
      if (off + n > pool.length) throw new Error('ENTROPY_POOL_EXHAUSTED');
      for (var k = 0; k < n; k++) arr[k] = pool[off + k];
      off += n;
      return arr;
    },
  };
  return pool.length;
};
